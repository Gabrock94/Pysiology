version: 2

# Build documentation in the following OS image
build:
  os: ubuntu-22.04
  tools:
    python: "3.11"

python:
  install:
    - method: pip
      path: .
      # This ensures your dependencies in setup.py are installed
    - requirements: docs/requirements.txt 

sphinx:
  configuration: docs/conf.py